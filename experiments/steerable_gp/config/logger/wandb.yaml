wandb:
    _target_: neural_diffusion_processes.utils.loggers_pl.WandbLogger
    save_dir: null #${paths.logs_dir}
    name: ${str:${seed}}
    group: ${name}_${hydra:job.override_dirname}
    # entity: oxcsml
    # entity: cbl-mlg
    entity: emilem
    # project: diffusion_manifold
    project: equiv-stochastic-diffusion-processes
    offline: False